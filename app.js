// JavaScript code for the demo game website

// Add an event listener for the "Play Now" buttons
const playButtons = document.querySelectorAll('.game button');
playButtons.forEach((button) => {
  button.addEventListener('click', (event) => {
    alert('Sorry, this is just a demo.');
    });
});
